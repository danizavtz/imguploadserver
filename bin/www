#!/usr/bin/env node
let server = require('../index.js');
let pg = require('../db/postgres');

pg.initialize(function(err){
  if(err){
    throw err;
  }
  server.listen(process.env.PORT);
  console.log('Microsservice upload listening at http://localhost:%s', process.env.PORT);
});